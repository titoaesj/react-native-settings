import {AppRegistry} from 'react-native';
import Root from './app/Root';
import moment from 'moment';

// Registrando o componente principal da aplicação
AppRegistry.registerComponent('ReactNativeSettings', ()=>Root);
